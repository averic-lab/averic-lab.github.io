# averic.co.kr — Averic Lab 사이트 (GitHub Pages)

안부(Anbu) 앱의 공식 웹사이트. **GitHub Pages**로 호스팅되며 `git push` 시 자동 배포된다.

| 항목 | 값 |
| --- | --- |
| 도메인 | `averic.co.kr` (CNAME) |
| 저장소 | `averic-lab/averic-lab.github.io` (branch `main`) |
| 배포 | `main`에 push → GitHub Pages 자동 게시 |
| 게시 엔진 | Jekyll(기본, `.nojekyll` 없음). **`_` 로 시작하는 폴더는 게시에서 제외됨** |
| 게시 제외 | `_config.yml`의 `exclude` — 현재 `CLAUDE.md` |

## 게시에서 제외하기 (`_config.yml`)

Jekyll은 front matter가 없는 `.md`도 **정적 파일로 그대로 복사**한다. 그래서 루트의 `CLAUDE.md`가 실제로 `averic.co.kr/CLAUDE.md`로 공개되고 있었고(커밋 `dd6987d`), 이를 막으려고 `_config.yml`에 `exclude`를 추가했다.

- **내부 문서를 새로 추가할 때는 두 방법 중 하나를 쓴다** — `_` 접두 폴더 안에 두거나(권장), `_config.yml`의 `exclude`에 추가한다.
- ⚠️ `exclude`를 지정하면 Jekyll **기본 제외 목록을 대체**한다. `_config.yml`에 기본값(`node_modules/`, `Gemfile` 등)을 함께 적어 둔 이유이니 지우지 말 것.
- `.nojekyll`을 추가해 Jekyll을 끄는 방법은 **쓰지 않는다** — `_` 폴더 제외 규칙까지 사라져 `_beta-test-build/`(스크린샷·빌드 스크립트)와 `_faq-build/`가 통째로 공개된다.
- ⚠️ **`_config.yml`을 고칠 때마다 배포 후 직접 확인할 것.** 이 파일이 잘못되면 문서가 아니라 사이트 전체가 깨진다. 확인 항목: `averic.co.kr/ko/` 정상 렌더 · `averic.co.kr/CLAUDE.md` 404 · `/test/`·`/preview/` 회귀 없음. (2026-08-06 커밋 `bd38607` 배포에서 전부 통과 확인)

## 검색 색인 (SEO) — 2026-08-07 점검

Search Console 「적절한 표준 태그가 포함된 대체 페이지 12건」을 계기로 전면 점검한 결과다.

**그 12건은 버그가 아니었다** — 전부 `http://averic.co.kr/{lang}/` 였고, GitHub Pages의 **Enforce HTTPS가 꺼져 있어** http 가 200으로 내용을 내주고 있었다. 페이지의 canonical 이 `https://` 를 가리켜 Google 이 그대로 존중한 정상 동작이다. **조치는 코드가 아니라 저장소 설정** — Settings → Pages → Enforce HTTPS 체크(2026-08-07 적용, `http://` → 301 확인). 같은 증상이 다시 보이면 코드를 뒤지기 전에 이 설정부터 확인할 것.

같이 발견해 고친 진짜 문제:

- **`sitemap.xml`·`robots.txt`가 아예 없었다.** 발행 100개 중 Google 이 찾은 건 71개뿐이었다. `python3 _seo/gen_sitemap.py`로 생성한다. ⚠️ **내용 변경을 먼저 커밋하고 그다음 사이트맵을 별도 커밋으로 올릴 것** — git 기록을 읽으므로 아직 커밋 안 된 변경은 `lastmod`에 안 잡힌다. 2026-08-07에 이 순서를 어겨 방금 고친 법적 페이지 40개의 `lastmod`가 두 달 전 날짜로 나갔다(재크롤링이 필요한 바로 그 페이지에 "5월 이후 안 바뀜"이라고 알리는 셈). 지금은 커밋 안 된 파일이 있으면 스크립트가 경고한다. **색인 대상만 넣는다** — noindex 페이지(루트 라우터·`preview/`·`test/`)는 넣지 않는다. 사이트맵에 넣고 noindex 로 막으면 모순된 신호가 된다. `lastmod`는 git 커밋 시각을 쓴다(파일 mtime 은 clone 마다 바뀌어 매번 전체가 갱신된 것처럼 보인다).
- **`privacy-policy.html`·`terms-of-service.html` 40개에 canonical·hreflang 이 하나도 없었다.** 이 둘만 손으로 쓴 페이지라 빌더가 없어서 빠져 있었다. `python3 _seo/patch_legal.py`로 넣는다 — **멱등**이라(기존 `<!-- seo:begin -->` 블록을 지우고 다시 넣음) 페이지를 새로 쓰거나 언어를 추가한 뒤 다시 돌리면 된다. 앱이 `/{locale}/privacy-policy.html`을 직접 열기 때문에 스토어 심사에서도 쓰이는 페이지다.
- **FAQ·사용설명에 `x-default`가 없었다.** `_faq-build/common.py`의 `head_links()`에 추가했다(홈과 같이 `en`을 가리킨다).

⚠️ **언어 목록·hreflang 코드는 `i18n/build.py`의 `ORDER`/`META`가 유일한 출처**다. `_seo/`의 두 스크립트도 그것을 import 한다 — 여기서 다시 정의하면 두 곳이 조용히 어긋난다.

**루트 `index.html`은 `noindex,follow`다(의도).** 언어 분기용 JS 라우터라 색인할 내용이 없다. `follow` + hreflang 이 있어 언어 페이지 발견에는 지장이 없지만, 그 결과 **맨몸 도메인 `averic.co.kr` 자체는 검색에 안 잡힌다**. 바꾸려면 라우터가 아니라 실제 내용을 가진 루트가 필요하다.

**페이지를 추가·수정한 뒤 확인** (100개 페이지 + 사이트맵 정합성):
- 각 페이지의 canonical 이 자기 자신을 가리키는가
- hreflang 20개 + `x-default`가 다 있고, 자기 자신을 상호 참조하는가
- 사이트맵의 URL 집합이 실제 파일 집합과 정확히 일치하는가

## 사이트 구조

- `index.html` — 루트 스플래시(언어 자동 분기), `style.css`, `site.js`
- `og-image.png` — 공유 미리보기(1200×630). 20개 언어가 이 한 장을 공유하므로 **문구는 언어 중립**으로 둔다(브랜드 + 도메인). 없으면 카톡·SNS 링크가 그림 없이 나간다
- `ko/`, `en/`, `ja/` … (20개 언어 폴더) — 각 언어별 `index.html` / `privacy-policy.html` / `terms-of-service.html` / `faq.html` / `guide.html`
- `i18n/` — 다국어 랜딩 빌드 도구(`build.py`, `template.html`, `translations.json`) — **20개 언어 `index.html` 전부를 생성**한다(단일 출처). `_` 접두가 아니라 Jekyll 기본 제외에 안 걸리므로 `_config.yml`의 `exclude`에 명시했다(2026-08-07 이전에는 `averic.co.kr/i18n/build.py`가 200으로 공개돼 있었다)
- `_seo/` — SEO 도구(게시 제외). `patch_legal.py`(법적 페이지 canonical·hreflang), `gen_sitemap.py`(sitemap.xml). 상세는 아래 별도 절
- `sitemap.xml` · `robots.txt` — **산출물이다. 직접 수정 금지**
- `_faq-build/` — FAQ·사용설명 빌드 도구 + `PRD-FAQ.md` (게시 제외). 상세는 아래 별도 절
- `CNAME` — `averic.co.kr`

## 한국어 줄바꿈 — `word-break: keep-all`

한국어·일본어·중국어는 기본적으로 **글자 단위로 아무 데서나 줄이 바뀐다.** "단계별로"가 `단계 / 별로`로, "사람,"이 `사 / 람,`으로 쪼개졌다. `h1`·`h2.section-title`·`p.section-sub`·`.hero-sub`·`.dawn-text .q1/.q2/.q3`에 `word-break: keep-all`을 걸어 단어 단위로만 끊기게 했다.

⚠️ **이 목록은 자동이 아니다.** 긴 문장을 새 자리에 넣을 때마다 그 선택자를 여기 추가해야 한다 — dawn 세 번째 문장은 앞의 두 줄보다 길어서 넣고 나서야 필요해졌다(2026-08-08).

- 라틴 문자는 원래 공백에서 끊기므로 **영향 없다**. 20개 언어 × 3개 폭에서 제목 넘침 0으로 확인.
- 안전망으로 `overflow-wrap: break-word`를 같이 둔다(공백 없는 초장문 단어 대비).
- ⚠️ **grid/flex 안에서는 `keep-all` 이 가로 넘침을 만든다.** 한국어는 띄어쓰기가 있어 안전하지만
  **일본어·중국어는 공백이 없어 절 하나가 통째로 끊기지 않는 "단어"** 가 되고, 그 `min-content` 가
  `1fr`(= `minmax(auto,1fr)`) 열을 밀어 올린다. 2026-09-03 「지킨 여섯 가지」에서 ja 380px 카드가
  402px 로 부풀어 페이지에 가로 스크롤이 생겼다. → `minmax(0,1fr)` + `min-width: 0`.
  `overflow-wrap: break-word` 는 **해결책이 아니다**(min-content 계산을 바꾸지 않는다).
  기존 `.step h3/p` 에 keep-all 이 없는 것도 같은 이유로 보인다. 상세는 「지킨 여섯 가지」 절.

## 「해질녘」 섹션 — 질문 세 개 (2026-08-08)

히어로 바로 아래, 배경만 있고 문장만 떠오르는 구간이다. 질문이 **순서대로** 하나씩 나타난다(`.q1` 0.2s → `.q2` 0.9s → `.q3` 1.6s, `.reveal`+`.in-view`).

1. 내가 아프거나 힘들 때, 가장 먼저 달려와 줄 사람은 누구일까요.
2. 그리고 지금 이 순간, 그 사람의 하루는 괜찮았을까요.
3. 가족들은 늘 "난 괜찮아"라고 말합니다. **하지만 정말 괜찮은 걸까요.**

- **③은 앞의 두 줄 뒤에 와야 한다.** 안으로 향하던 질문이 밖으로 꺾이는 전환이라, 순서를 바꾸거나 ①②를 지우고 ③만 남기면 힘이 빠진다.
- ③의 인용구는 19개 언어에서 **직역이 아니라 그 나라 관용구**다(`I’m fine` / `Ça va` / `大丈夫だよ` / `У меня всё хорошо`). 새 언어를 추가할 때 사전적으로 옮기지 말 것.
- 질문을 하나 더 넣으려면 `.q4`의 **등장 지연 + `margin-bottom`** 을 같이 넣어야 한다(지연이 없으면 ①과 동시에 뜨고, 여백이 없으면 앞 문장에 붙는다). 구분선 `.mark`도 문장 사이마다 하나씩 있다.
- 이 문장들은 `word-break: keep-all` 대상이다(위 절 참조).

## 히어로 세로 공간 — 첫 화면 안에 CTA가 들어와야 한다

제목이 「혼자 지내는 소중한 사람, 매일 걱정하고 계시나요?」로 길어지면서 두 가지를 조정했다:

- **제목 크기 상한 56px → 40px.** 56px에서는 3~4줄로 늘어 스토어 배지와 가격 문구가 첫 화면 밖으로 밀렸다.
- **히어로 단일 열 전환 분기점 880px → 1000px.** 1000px 아래에서는 두 칸을 유지하면 카피 열이 좁아져 제목이 4줄이 된다. 나머지 섹션은 880px 그대로다(히어로만 별도 블록).
- 긴 언어(러시아어·네덜란드어·베트남어 등)는 제목 문구 자체를 짧게 다듬었다. 이 자리는 **두 줄이 상한**이다.

## 「도움이 필요해요」 섹션 — 긴급 요청 흐름 (2026-08-07)

기존 「적당한 거리」(개인정보) 섹션을 대체했다. **설명 문장 없이 흐름만** 보여준다 — 폰 3대가 나란히 선다.

`① 대상자 앱 홈 → (긴급 버튼 탭 → 확인 창 → 전송 탭)` → `② 보호자 알림(위치 보기)` → `③ 위치 지도`

- ⚠️ **한 폰에서 여러 장을 순환시키지 말 것**(2026-08-07 4장 순환 → 4대 분리 → 3대로 두 번 고쳤다). 순환은 지나간 단계를 지워버려 흐름 전체를 동시에 보여주지 못한다. 단계마다 **자기 폰**을 갖는 지금 구조가 요구사항이다.
- ⚠️ **①의 홈 화면은 사용설명(guide)의 안전 홈 UI 전체**다 — 앱바, 코드 카드(복사·공유 버튼 포함), 보고 상태 카드, 시각 변경 행, 안전 보고 행, 긴급 버튼. 요약해서 줄이지 말 것.
- ①과 ②는 원래 별도 폰이었으나 **하나로 합쳤다** — 확인 창은 같은 화면 위에 뜨는 것이라 폰을 나누면 "화면이 바뀐 것"처럼 잘못 읽힌다. 창은 ①의 4초 구간 안에서 `버튼 탭 → 창 등장 → 전송 탭` 순으로 열렸다 닫힌다.
- 폰 아래 **`대상자 앱` / `보호자 앱` 라벨**(`flow_lbl_subject` / `flow_lbl_guardian`, `translations.json` 20개 언어)이 붙는다. 번호만으로는 도중에 기기가 바뀐다는 게 안 보인다.
- 강조가 4초씩 3단계를 돈다(12초 주기). `.fs1~.fs3`의 `--d`(지연)를 `.fstep`(들어올림)·`.fnum`(번호 점등)·`.fphone`(그림자)·확인 창·탭 표시가 **공유**한다 — 지연을 개별 규칙에 흩뿌리지 말고 `--d`(와 `.tapb`/`.tapc`의 고정 지연)만 고칠 것.
- 탭 표시는 `.tapa`(긴급 버튼, 0s) / `.tapb`(전송, 2.5s) / `.tapc`(위치 보기, 6.2s) **셋이 서로 다른 시점**에 눌린다. 하나의 애니메이션으로 묶으면 동시에 눌려 이야기가 깨진다.
- **크기 조절은 `.fstep`의 `--s`(scale)로만** 한다. `.fphone` 내부 좌표는 300×620 기준 실측값이라 내부 px 을 손대면 화면이 어긋난다.
- **라벨은 `padding-top`으로 확보한 폰 자리 아래에 static 으로 흐른다.** `position:absolute; bottom:0`으로 두면 두 줄이 되는 언어(터키어 `Takip edilen kişinin uygulaması` 등)에서 위로 자라 폰을 침범한다. `min-height:2.7em`으로 두 줄을 항상 확보해 한 줄 언어와 첫 줄 높이를 맞춘다.
- 클래스명에 **`f` 접두사가 붙은 이유**: 페이지에 이미 `.step`/`.step-num`(reveal 대상)이 있어 `.step`을 쓰면 그쪽 `opacity:0`을 물려받아 폰이 통째로 안 보인다. 마크업을 넣은 뒤 CSS 를 쓰기 **전에** 클래스명이 `style.css`/`site.js`의 다른 섹션과 겹치는지 검사할 것. 단 `ncard`/`nrow`/`nic`/`nc-*`/`nbody`는 「단계별 알림」과 **의도적으로 공유**한다.
- 화살표(`.farrow`)는 **한 줄일 때만** 쓴다. 접히면 방향이 어긋나므로 숨기고 번호와 라벨이 순서를 맡는다. RTL은 flex 가 순서를 뒤집어 주므로 촉만 `scaleX(-1)`.
- 폭 구간: `>1080` `--s .72` / `~1080` `--s .6` / `~820` 접힘 `--s .62` / `~620` `--s .5` / `~400` `--s .42`. **820px 까지 한 줄을 유지**하는 건 축소가 접는 것보다 순서를 잘 전달하기 때문이다.
- 화면 문구는 전부 앱 번역 파일에서 온다(`APP_SOS_*` / `APP_MAP_*` / `APP_CHECK_*` / `APP_SCHED_*`). 확인 창 본문의 `\n`은 `build.py`가 `<br>`로 바꾼다. 사용설명은 상태 카드 제목에 오늘 날짜를 JS 로 붙이지만 **홈은 붙이지 않는다** — 빌드 시각에 문자열이 박히므로 날짜를 넣으면 저절로 낡는다.
- **지도는 거리 수준**이다(`.fmapsvg`). 국가 단위 지도는 화면의 `정확도 12 m` 표시와 축척이 안 맞아 교체했다. 격자만 그리면 표처럼 읽히므로 **대각 대로와 강**을 넣었고, 강은 `건물 위 · 도로 아래`에 깔아 교차부가 다리처럼 보이게 순서를 잡았다 — 이 순서를 바꾸면 건물이 강 위에 올라타 물이 조각나 보인다. 핀은 `<circle>`로 **SVG 안에** 둬서 지도와 같이 스케일된다(바깥에 두면 화면 크기마다 위치가 어긋난다). 도로선은 viewBox 밖까지 그어 SVG 가 잘라내게 하므로, 넘침 검사기는 `el.ownerSVGElement`인 요소를 건너뛰어야 한다.
- `prefers-reduced-motion`에서는 강조만 끈다. 3장이 이미 다 보이므로 **흐름은 그대로 전달된다**. 단 확인 창은 애니메이션이 곧 표시라 켜진 상태로 고정한다.
- 검증 스크립트는 스냅샷 한 장으로 시퀀스를 못 본다. **시점별 프로브**(0.6/1.4/2.4/3.2/5.0/6.4/9.5초)로 창 불투명도·들어올린 폰이 하나뿐인지·각 탭이 자기 버튼 안에 있는지를 확인할 것.

⚠️ **이 섹션이 개인정보 설명을 대체했다.** "위치는 [도움이 필요해요]를 눌렀을 때만 전달된다"는 문장이 사라졌으므로, 개인정보 관련 설명이 필요하면 `{lang}/privacy-policy.html`이 담당한다.

## 「지킨 여섯 가지」 섹션 — 타사와의 차이를 부정형으로 (2026-09-03)

`intro`(추상적 약속) **다음**, `levels`(작동 방식) **앞**. 경쟁 앱 조사에서 나온 차이를
**타사를 거명하지 않고** 담는 자리다. 폰 목업이 없는 유일한 카드 섹션이다(이미 4곳에 있어 더 넣으면 지친다).

- ⚠️ **항목 문구는 `~하지 않으셔도 됩니다` 부정형을 유지할 것.** 그럴 필요가 있는 상황이
  존재해야만 성립하는 문장이라, 비교 대상을 쓰지 않고도 차이가 전달된다. 긍정형("자동으로
  전달됩니다")으로 바꾸면 조사 전에도 쓸 수 있던 평범한 기능 설명으로 되돌아간다.
- ⚠️ **02 는 "동일하게 / 기능 제약 없음" 을 주장하지 않는다.** iOS 는 모드 선택 화면이 없어
  **대상자 모드로 시작할 수 없고**(G+S 로만 가능) 경로가 실제로 다르다. "설치해 쓸 수 있다"
  까지만 말하고 동작 방식은 주장하지 않는다. 20개 언어 모두 "둘 중 하나에서만 되지 않는다"
  (가용성)이지 "기능이 같다"(동등성)가 아니다.
- ⚠️ **연령 지칭 금지**(어르신·독거노인 등). 이 앱은 고령자 전용이 아니며 당사자도 그 단어를
  꺼린다. 홈 카피(`translations.json`)에는 원래 0건이었다 — 새로 들이지 말 것.
  (`ko/terms-of-service.html` 의 "독거노인 및 1인 가구"는 손으로 쓴 법적 페이지라 별건이다.)
- **`보호자`는 사이트의 기능 어투를 쓴다** — `flow_lbl_guardian` 기준
  (en `family` / ja `見守り人` / zh `家人` / fr `proche` / es·it `familiar(e)` / ru `близкий` …).
  ⚠️ 사이트에는 감성 어투("your loved ones" / `deine Liebsten` / `vos proches`)도 있었는데
  (삭제된 intro 카드 `step1_p`), 이 섹션은 **제품 됨됨이**를 말하는 자리라 기능 어투가 맞다.
- **`안부`도 이미 정해진 말이 있다** — `hero_sub_html` 기준(de `Wohlbefindens-Meldung` /
  fr `signe de vie` / sv `livstecknet` / nl `het teken` / ru `сигнал` / tr `iyilik haberi` /
  hi `खैरियत` / zh-CN `安好信号` / ja `安否`). 새로 지어내면 같은 개념이 언어마다 달라진다.
  특히 de 를 `Lebenszeichen`(생존 신호)으로 쓰지 말 것 — 등록부가 과하게 무겁다.

### 형태는 **흰 카드 6장**이다 — 세로 목록으로 되돌리지 말 것

한때 카드 → **세로 목록** → 다시 카드로 두 번 뒤집혔다. 이유가 그때그때 달랐다:

1. 처음엔 카드였는데 **당시 intro 에도 흰 카드 2장이 있어** 구조가 똑같아졌다
   (eyebrow+제목+부제+카드 그리드). 그래서 세로 목록으로 바꿨다.
2. 그 뒤 **intro 카드를 삭제**하자(아래) 흰 카드가 이 섹션 전용이 되어 겹칠 일이
   없어졌고, 텍스트만 남은 intro 와 대비가 오히려 더 뚜렷해져 카드로 되돌렸다.

⚠️ **다시 목록으로 바꾸고 싶어지면 intro 에 카드가 있는지부터 확인할 것.** 지금은 없다.

**6장이라 2열 × 3행으로 딱 떨어진다.** 한때 5장이던 시절에는 마지막 항목을 전체 너비
(`.made-item.wide`)로 뒀는데, 06 이 들어오면서 불필요해져 그 클래스를 제거했다.
⚠️ 항목이 홀수가 되면 마지막이 왼쪽에 홀로 남으니 그때 다시 필요하다.

⚠️ **맥박(`madePulse`)은 `:nth-child(2)` 에 걸려 있다.** 그 항목이 「앱을 종료하셔도 안부는
멈추지 않습니다」이기 때문이다 — 장식이 아니라 **그 문장의 시각적 대응**이다.
**항목 순서를 바꾸면 이 선택자도 같이 옮길 것.** `:first-child` 로 두면 엉뚱한 항목이 뛴다.
(2026-09-03 에 01 로 「20개 언어」가 들어오면서 한 칸 밀렸다.)

### 01「20개 언어, 그 나라의 시각 그대로」— 주장 근거

- **20개 언어**: 앱 UI·서버 Push·OS 권한 팝업(`InfoPlist.strings`)·이 웹사이트 전부.
- **각자의 언어로 도착**: 서버가 `devices.locale` 을 보고 수신자별로 Push 문구를 만든다
  (PRD-FrontEnd §12.3). 그래서 보내는 쪽과 지켜보는 쪽의 언어가 달라도 각자 자기 언어로 받는다.
- **시각 표기**: 앱 `time_utils.dart` 의 `TimeStyle` — `pre12`(오후 3:12, 한·일·중) /
  `post12`(3:12 PM, 영어·아랍어) / `h24`(15:12, 나머지 14개). **기기의 24시간제 설정이
  언어 설정보다 우선**한다.
  ⚠️ 본문이 **세 가지 표기를 실제로 나란히 보여주고**(오후 6:00 / 6:00 PM / 18:00)
  마지막에 "기기 설정이 언어보다 우선"까지 말한다. 추상적으로 "현지 표기를 따릅니다"
  라고만 쓰면 무엇이 달라지는지 전달되지 않아, 일부러 예시를 넣은 것이다 — 줄이지 말 것.
  ⚠️ **비-한국어 페이지에 한글 `오후 6:00` 을 넣지 말 것**(2026-09-03 정정). 한때
  "그 표기가 어떻게 생겼는지 보여주는 것이 요점"이라며 16개 언어에 그대로 넣었는데
  **틀렸다** — 독일어·러시아어 독자는 그 글자를 읽지 못해 아무것도 얻지 못하고,
  CJK 폰트가 없는 환경에서는 □□(두부)로 보여 사이트가 깨진 것처럼 읽힌다.
  지금은 **읽을 수 있는 두 예시**(`6:00 PM` / `18:00`)만 쓴다. `ko`·`ja`·`zh` 는
  자기 문자이므로 각자의 표기(`오후 6:00` / `午後6:00` / `下午 6:00`)를 그대로 둔다.
  → 예시는 **독자가 읽을 수 있을 때만** 예시다.
- **시간대**: `devices.timezone` 기준. 미수신 체크(예약시각 +2h)도 현지 시각으로 돈다.

### ⚠️ 05(개인정보) — 문구를 강하게 만들지 말 것

*"개인정보를 절대 수집하지 않습니다"* 로 쓰고 싶어지지만 **사실이 아니다.** 실제로 받는 것:
device_id·걸음수·배터리·locale, **긴급 요청 시 위치**(최대 24시간)와 선택 메시지,
보호자가 붙인 별칭(Push 렌더링용 사본, `guardians.alias`).

- 쓸 수 있는 표현은 **"서버에 이름이나 전화번호 같은 개인정보를 저장하지 않습니다"**
  (PRD §1.6 의 공식 표현) 까지다.
- ⚠️ **마지막 줄("평소에는 위치도 보지 않습니다 — 도움을 요청하셨을 때만 한 번")을 빼지 말 것.**
  같은 페이지 아래에 「버튼 하나로 현재 위치까지 전해집니다」 섹션이 있어, 이 한정이 없으면
  **정면 모순**이 된다. 이 줄이 그 섹션의 전제를 미리 깔아 주는 역할도 한다.
- 「회원가입이 없다」는 **넣지 않았다** — 히어로 3번째 항목과 04 가 이미 말한다(세 번째 반복).

### CSS 함정 넷 — 전부 실제로 밟았다

1. **배경은 `var(--warm-paper)`. 순수 흰색은 카드 전용(`var(--paper)`)이다.**
   이 사이트는 `intro` 끝(warm-paper) → `levels` 시작(warm-paper) 으로 이어지는 **색 사슬**이라,
   섹션에 `#ffffff` 를 쓰면 위아래가 뚝 끊긴다. 관례가 정확히 반대다.
2. **접두사가 `.made*` 인 이유** — 만들 당시 intro 에 `.step`/`.steps`(2장 그리드,
   `opacity:0`)가 있어 재사용하면 4장 배치가 깨지고 카드가 통째로 안 보였다.
   그 클래스들은 2026-09-03 에 삭제됐지만 **교훈은 유효하다: 새 섹션은 마크업을 쓰기
   전에 클래스명 충돌을 `grep` 으로 검사할 것.**
3. **`.reveal` 은 `site.js` 가 `.in-view` 를 붙이는 훅일 뿐이다.** 전환 효과는 컴포넌트가
   직접 정의해야 한다(`.step` 과 같은 방식). 안 하면 스크롤 등장이 아예 없어 정적으로 보인다.
   ⚠️ `:hover` 규칙은 `.in-view` 와 특이도가 같으므로 **반드시 뒤에** 와야 한다.
4. **⚠️ `word-break: keep-all` + CJK + grid = 가로 넘침.**
   `keep-all` 은 한국어(띄어쓰기 있음)에는 좋지만 **일본어·중국어는 공백이 없어 절 하나가
   통째로 끊기지 않는 "단어"** 가 된다. 그 `min-content` 가 `1fr`(= `minmax(auto,1fr)`)
   열을 밀어 올려, ja 380px 에서 카드가 402px 로 부풀고 페이지에 가로 스크롤이 생겼다.
   → `grid-template-columns: repeat(2, minmax(0,1fr))` + 아이템에 `min-width: 0`.
   (`overflow-wrap: break-word` 만으로는 안 된다 — 그건 `min-content` 계산을 바꾸지 않는다.)
   기존 `.step h3/p` 에 keep-all 이 **없는 것도 같은 이유**로 보인다.

### 번역 품질 — 반복해서 걸린 것들

20개 언어를 손볼 때마다 같은 함정에 걸렸다. 새 문구를 넣기 전에 이 목록을 볼 것.

1. **용어 드리프트.** 같은 개념을 항목마다 다른 단어로 옮기게 된다. `안부` 는
   `hero_sub_html`, `보호자` 는 `flow_lbl_guardian` 이 **유일한 기준**이다.
   실제로 02 에서 `平安信号`(hero 는 `安好信号`), 04 에서 `tín hiệu`(hero 는
   `tín hiệu bình an`) 로 두 번 어긋났다.
2. **3인칭 전환.** 카드가 독자를 직접 부르는데 한 문장만 "사용자/使用者/ผู้ใช้" 로
   바뀌어 시점이 튄다. 태국어·중국어에서 실제로 났다.
3. **이중부정.** 한국어 `한쪽에서만 되거나 … 없습니다` 를 로망스·슬라브어로 직역하면
   *"둘 중 하나에서만 작동하지 않는다"* 가 되어 독자가 잠깐 "작동을 안 한다고?" 로
   읽는다. 그 절은 **빼는 것이 맞다** — 앞 문장이 이미 같은 말을 한다.
4. **등록부(register).** 일본어 `押しつけません`(대결적) → `〜していただく必要は
   ありません`, 독일어 `durchgespielt`(게임) → `erprobt`, `dazwischenkommt`(약속이
   틀어질 때) → `passiert`. 뜻은 맞아도 그 상황에 안 쓰는 말이 있다.
5. **숫자 세는 말.** 안전 코드는 `K7M-4PXR`(영숫자)인데 중국어 `7 位`/`7 碼`,
   러시아어 `семизначный` 는 **숫자 7자리**로 읽힌다. `7 位字符` / `из семи символов`.
6. **음차어 혼입.** 힌디어에서 나머지가 고유어인데 `सेफ़्टी कोड` 만 영어 음차였다
   (`सुरक्षा कोड`).

### ⚠️ `en` 폴백은 `translations.json['en']` 이 아니라 `i18n/strings.en.json` 이다

`build.py`(223행)가 `strings.en.json` 을 읽어 기준으로 삼고, 언어별 **비어 있지 않은** 값만
덮어쓴다. 그래서 새 키를 `translations.json['en']` 에만 넣으면 **나머지 19개 언어에서
`{{token}}` 이 그대로 남는다**(빌드의 미해결 토큰 검사가 잡아 준다). 새 키는 반드시
`strings.en.json` 에도 넣을 것. 한 언어만 먼저 작업할 때는 이 폴백 덕에 나머지가 영어로 나온다.

## 「우리는」(intro) 섹션 — 카드를 없애고 선언만 남겼다 (2026-09-03)

심장 SVG + eyebrow + 제목 + 부제. **카드는 없다.**

원래 `.steps` 안에 흰 카드가 있었다(3장 → 2장 → 0장). 마지막 2장을 지운 이유:

- **01「매일 자동으로 안부」는 「지킨 여섯 가지」 01 및 히어로와 같은 말**이었다.
- **02「그리고 안심」의 본문은 절반이 히어로 중복**이다 — *"정말 필요한 순간에만 직접
  연락하세요"* ≈ 히어로 부제의 *"전화나 방문 확인은 필요할 때만 하세요"*. 나머지 절반
  *"단계별로 간접적으로 알 수 있습니다"* 의 `단계별로` 는 독자가 아직 모르는 용어이고,
  바로 다음다음 `levels` 섹션이 그것을 통째로 설명한다.
- ⚠️ 그래서 **02 본문을 부제로 끌어올리는 것도 하지 말 것** — 히어로 중복이 같은 화면
  안으로 들어오고, `단계별로` 가 설명 없이 흐르는 문단에 섞인다.

결과적으로 섹션마다 역할이 하나씩 떨어진다:
`dawn`(감정) → `intro`(선언) → `made`(원칙) → `levels`(작동).

함께 삭제된 것: `step1_h3`·`step1_p`·`step1_android`·`step1_iphone`·`step2_h3`·`step2_p`·
`step3_h3`·`step3_p` 키(`translations.json`·`strings.en.json`)와 `style.css` 의
`.steps`/`.step`/`.step-num`/`.step:hover`/`[dir="rtl"] .step` 규칙 47줄.
⚠️ `_faq-build/guide-template.html` 도 `.step`/`.steps` 를 쓰지만 **자체 인라인 CSS** 라
`style.css` 를 불러오지 않는다 — 그래서 삭제해도 안전했다. 확인 없이 지우지 말 것.

## 「최대 5명」 섹션 — 연결 관리 → 대시보드 (2026-08-08)

「도움이 필요해요」 **바로 앞**에 있다. 폰 2대가 한 이야기를 나눠 갖는다 — ① 연결 관리에서 대상자를 추가하면 ② 대시보드에 카드가 한 장 늘어난다. **18초 주기**(①이 0~9초, ②가 9~18초), CSS 애니메이션만 쓴다(JS 없음).

- ⚠️ **②의 카드는 전부 「안전(정상)」이다.** 이 섹션의 주제는 **인원 한도**이지 경고 등급이 아니다. 등급 이야기는 「단계별 신호」가 따로 맡으므로 주의/경고/긴급 카드·색·문구, 푸시 배너를 여기로 끌어오지 말 것 — 히어로 목업에서 마크업을 복사하면 `.plegend`의 caution 칩, `.scard.c2`, `pop2`(안전 확인 필요), `APP_PUSH_*`가 딸려온다. 검증 스크립트가 `.ks2` 안에 경고 계열 색·문구가 있는지 확인한다.
- **클래스 접두사는 `k`**. `.c`(히어로 코드카드)·`.f`(긴급 흐름)·`.n`(알림)·`.p`(히어로 폰)이 이미 쓰여 남은 것을 골랐다. 마크업을 넣기 **전에** `grep`으로 충돌을 검사할 것(`.step` 사고와 같은 이유).
- ⚠️ **범례의 초록 점은 `.kleg > span > i` 로 잡는다.** 자식 선택자를 빼면 `.kswap` 안의 숫자 `<i>`까지 7px 원으로 뭉개져 카운터가 사라진다(실제로 그렇게 나갔다가 고쳤다).
- **`.kscreen`은 세로 flex**여야 한다 — 오른쪽 폰은 `.kbar/.kappbar/.kpad/.knav`가 직접 자식이라, flex가 없으면 `.kpad`가 남는 높이를 못 먹어 **하단 네비가 화면 중간에 뜬다**. 왼쪽 폰의 `.kscr`은 absolute라 영향받지 않는다.
- **왼쪽 폰은 화면 두 장을 겹쳐 두고 crossfade** 한다(`.kconn`/`.kadd`). 입력값(`.kv1~3`)은 힌트 없이 **빈 칸 → 값 등장**으로 "입력 중"을 표현한다 — 힌트와 값을 같이 두면 두 문자열이 같아 구분이 안 된다.
- **대시보드 카드 넘김은 실제 `overflow-x` 스크롤이 아니라 `.ktrack`의 `translateX`다.** 장식(`aria-hidden`)이 사용자 입력을 받으면 안 되고, 좁은 폭에서 좌표가 어긋난다(히어로 `.pwrap`과 같은 이유). RTL은 부호를 뒤집은 `kSlideRtl`을 쓴다.
- **접힘 구간이 없다.** 폰이 2대뿐이라 380px에서도 `--s .38`로 한 줄이 유지된다 — 접으면 ①→② 순서가 흐려지므로 축소를 택했다(3대인 「도움이 필요해요」는 820px에서 접는다).
- 카운터가 두 번 바뀐다: **왼쪽이 먼저**(등록 직후 ≈6.5초, `kOldL`/`kNewL`), **오른쪽이 나중**(9초, `kOld`/`kNew`). 이 시차가 "등록했더니 대시보드에 반영됐다"는 이야기다 — 같은 시점으로 맞추지 말 것.
- 화면 문구는 전부 앱 번역 파일에서 온다(`APP_CONN_*`/`APP_ADD_*`/`APP_CHECKING3`·`4`). `@current`/`@max`/`@time`은 `build.py`의 `app_tokens()`가 3·4/5/18:00으로 채운다.
- 검증은 스냅샷 한 장으로 안 된다. **시점별 프로브**(0.5/1.6/3/4.8/6/7.5/9.5/12/14.5초)로 화면 두 장이 동시에 보이지 않는지·4번째 카드가 등장 전에 보이지 않는지·카운터가 실제로 3→4로 바뀌는지를 확인할 것.

## 「단계별 신호」 섹션 — 알림 페이지 폰 목업 (2026-08-07)

왼쪽 설명 + 오른쪽 폰. 히어로와 두 가지가 다르다:

- **폰이 정면이다**(3D 회전·perspective 없음). 히어로가 이미 기울어진 폰을 쓰므로 같은 각도를 반복하면 한 페이지에 같은 장치가 두 번 나온다.
- **알림 카드가 좌·우로 번갈아** 튀어나온다(`.np1`~`.np5`, 왼–오–왼–오–왼). 좌우 교차가 목적이므로 `left` 값을 한쪽으로 몰지 말 것.
- 떠난 자리는 히어로와 **같은 규칙**으로 반투명 유리(`.nlist .ncard::after`)가 덮는다. 5개 등급이 모두 튀어나오므로 예외는 없다.
- **아이콘은 앱 매핑 그대로** — 정상 `check_circle` · 주의 `info`(ⓘ, 느낌표 아님) · 경고 `warning_amber`(삼각형) · 긴급 `error`(원 안 !) · 걸음수 `directions_walk`. 앱은 `messageKey == 'steps'`를 등급보다 **먼저** 분기하므로 걸음수는 정보 등급이어도 종 아이콘이 아니라 걷는 사람이다.
- 카드 문구는 전부 앱 번역 파일에서 온다(`APP_NOTI_*` / `APP_LV_*`). 자리표시자 `@days`·`@steps`는 `build.py`의 `app_tokens()`가 3일·3,482보로 채운다.
- ⚠️ **가로로 508px이 필요하다**(폰 300 + 좌우 104씩). 두 칸 배치로는 1080px 아래에서 그 폭이 안 나오므로 세로로 쌓고, 더 좁아지면 `.nwrap`을 통째로 `scale()` 한다 — 카드 좌표가 고정값이라 개별로 줄이면 어긋난다(히어로 `.pwrap`과 같은 이유).

## 홈 헤더 메뉴 — 가로 스크롤

단어가 긴 언어(독일어 `Nutzungsbedingungen`, 러시아어 등)에서 좁은 화면일 때 메뉴가 넘쳐 **오른쪽 끝의 언어 버튼이 잘렸다**(2026-08-07 실기기 확인).

- **스크롤은 `.nav-links`(링크 4개)에만 걸고, `.lang-toggle`은 `flex: 0 0 auto`로 고정한다.** 줄 전체를 스크롤시키면 정작 필요한 언어 버튼이 화면 밖에 남아 문제가 그대로다 — 이 분리를 합치지 말 것.
- 스크롤 자체는 모든 폭에 걸어 두지만 **넘칠 때만 동작**한다(데스크톱은 독일어도 366/366으로 딱 맞아 스크롤이 생기지 않는다). 그래서 별도 분기가 필요 없다.
- 오른쪽 끝 페이드(`mask-image`)는 **≤720px에서만** 준다 — 넘치지 않는 폭에서 페이드를 주면 멀쩡한 마지막 글자가 흐려 보인다.
- 스크롤바는 숨긴다(`scrollbar-width:none` + `::-webkit-scrollbar`).

## 언어 자동 분기 (루트 `index.html`) — 불변 규칙

루트는 클라이언트 JS로 언어를 골라 `/{code}/`로 `location.replace` 한다. 우선순위:

1. `?lang=xx` — **저장까지 갱신한다.** `averic.co.kr/?lang=ko` 링크 하나로 영구 교정할 수 있어야 하기 때문
2. 저장된 선택 (`localStorage['anbu.lang']`, **90일 만료**)
3. `navigator.languages` (사용자 선호 순서대로 첫 매치)
4. `/en/`

⚠️ **저장된 선택은 기기 언어를 덮어쓴다.** 2026-08-07 이전에는 만료도 없고 되돌릴 UI도 없어서, 홈 스위처에서 언어를 한 번 누르면 그 브라우저에서는 **영원히** 그 언어로 열렸다 — 한국어 폰인데 일본어로 뜨는 현상의 원인이었다(기기 언어 감지 자체는 정상이었다). 고친 것 셋:

- `?lang=`이 저장값도 갱신 → 교정 가능
- 90일 만료
- 예전 형식(코드 문자열만, 시각 없음)은 읽는 즉시 폐기 → **이미 갇힌 사용자도 다음 방문에 자동 복구**

**저장 형식은 `index.html`과 `site.js` 두 곳이 맞춰야 한다** — `{"code":"ko","t":1786…}`. `site.js`가 예전처럼 문자열만 넣으면 만료 판정이 안 돼 같은 고착이 재발한다.

> 참고: `faq.html`·`guide.html`은 `site.js`를 싣지 않으므로 **그쪽 스위처는 언어를 저장하지 않는다.** 저장은 홈 스위처에서만 일어난다.
>
> 앱은 개인정보·이용약관을 `/{기기언어}/…`로 직접 열어 이 라우터를 타지 않는다. 설정 하단 `www.averic.co.kr`만 루트로 오며 `averic.co.kr`로 301된다(같은 오리진이라 저장값이 갈리지 않는다).

## 홈 히어로 — 폰 목업 (2026-08-07)

히어로 오른쪽의 3D 폰 목업은 **앱 화면을 HTML/CSS로 재현**한 것이다. 스크린샷을 쓰지 않는 이유는 FAQ·사용설명과 같다(`_faq-build/PRD-FAQ.md` §1).

**폰 안의 문구는 전부 앱 번역 파일에서 추출한 값이다.** `i18n/build.py`가 `_faq-build/app-strings.json`을 읽어 `APP_*` 토큰으로 넣는다 — 20개 언어가 공짜이고, 앱 문구가 바뀌면 `extract_strings.py` 재실행 한 번으로 따라온다.

```
앱 저장소 translations/*.dart
   → _faq-build/extract_strings.py → app-strings.json
       ├→ build_faq.py / build_guide.py   (FAQ·사용설명)
       └→ i18n/build.py                    (홈 히어로 목업)   ← 2026-08-07 추가
```

- `LANG_TO_STRINGS`(사이트 코드 → 앱 파일 코드)의 **단일 출처는 `i18n/build.py`**다. `_faq-build/common.py`가 `build.py`를 임포트하므로 반대 방향은 순환이 된다 — 옮기지 말 것.
- 자리표시자(`@count`, `@hours`)는 `build.py`의 `app_tokens()`가 직접 채운다(2명 / 2시간). 채우지 않으면 사용자에게 `@count`가 그대로 보인다.
- ⚠️ **목업 좌표는 실측값이다.** 떠오른 카드(`.pop` / `.pop2` / `.codecard`)의 `left`/`top`은 폰 **352×722** 기준으로 원래 자리(`.slot`/`.slot2`)를 측정해 박아 둔 값이다. 폰 크기를 직접 바꾸면 카드가 제자리를 벗어난다 — 좁은 화면에서는 **`.pwrap`을 통째로 `scale()`** 하고 남는 세로 공간을 음수 margin으로 회수한다(반응형 블록 참조).
- 떠오른 카드가 남긴 자리는 **같은 내용 + 반투명 유리(`.glass`)**로 덮는다. "어디서 나왔는지"를 보여주는 장치이므로 유리를 지우지 말 것.
- `.push`(푸시 알림)는 고정 헤더(`z-index:100`, 높이 64px)를 가리지 않도록 `top:78px`로 내려 두었다. 위로 올리면 20개 언어 전부에서 헤더 메뉴를 덮는다.
- 폰 목업은 `aria-hidden="true"`다 — 스크린리더에는 장식이다.

### 목업 카드의 별칭 — `nick1`~`nick4` (2026-08-08)

대상자 이름은 원래 `A`·`B`·`C`·`D`였는데 **별칭인지 그냥 알파벳인지 구분되지 않아** 각 언어의 실제 호칭(`엄마`/`Mom`/`Мама`…)으로 바꿨다. `translations.json`의 `nick1`~`nick4`가 단일 출처이고 **홈 전체가 공유**한다 — 히어로 카드(`.nm` 2장), 긴급 위치 지도의 `대상자` 값, 「최대 5명」의 연결 카드 4장·대시보드 카드 4장·입력 화면의 별칭 값. 한 곳만 알파벳으로 되돌리면 같은 페이지 안에서 같은 화면이 어떤 곳은 `A`, 어떤 곳은 `Mom`으로 나온다.

- 앱 문구가 아니라 **사이트 카피**다(사용자가 직접 입력하는 값이라 앱 번역 파일에 없다). `app-strings.json`이 아니라 `translations.json`에 둔다.
- 가장 긴 값은 `Бабушка`·`Babaanne`·`Farbror`(7~8자)다. 카드 이름 칸은 이 폭까지 확인했다 — 더 긴 호칭을 넣으면 배터리·등급 배지와 부딪힌다.
### 안전 코드 카드의 복사·공유 버튼 + 점선 설명

- ⚠️ **`.co`는 반드시 `width: 0`이어야 한다.** 폭을 가지면 `inset-inline-end`가 점선이 아니라 **상자의 끝**을 잡아서, 점선이 **라벨 폭의 절반만큼 왼쪽으로 밀린다** — 라벨 길이가 언어마다 달라 어긋남도 언어마다 달라진다(2026-08-07 실제 발생). 점선은 버튼 중심에 정확히 서야 한다.
- 좌우 기준은 `right`가 아니라 **논리 속성 `inset-inline-end`** — 아랍어에서 카드·버튼이 반대편으로 넘어갈 때 점선도 따라가야 한다. 값(복사 72px / 공유 32px)은 카드 padding 16 + 버튼 32 + 간격 8에서 나온 **버튼 중심까지의 거리**다. 버튼 크기·여백을 바꾸면 같이 고쳐야 한다.
- 두 점선의 **길이를 크게 어긋내 둔 것은 의도**다(복사 104px / 공유 56px). 버튼 간격이 36px뿐이라 라벨을 같은 높이에 두면 언어에 따라 겹친다. 맞추지 말 것.
- **아랍어(RTL)는 꺾은선**이다 — 위로 올라간 뒤 오른쪽으로 꺾어(`ㄱ`의 좌우 반전) 라벨을 폰 **바깥**으로 뺀다. RTL에서는 카드가 화면 왼쪽으로 가면서 라벨이 폰 화면을 덮기 때문이다. 꺾이는 방향은 **화면 기준**이라 이 블록에서만 물리 속성(`left`)을 쓴다.
- RTL에서 `.cohead`에 **`direction: ltr`**를 주는 이유: RTL 컨테이너에서는 flex `row`도 오른쪽→왼쪽이라 라벨이 다시 폰 위로 되뻗는다. 상자 순서만 물리적으로 고정하고 아랍어 본문 방향은 `.cotext`에서 `direction: rtl`로 되돌린다.
- **`.pwrap`의 `margin-right`(262px)는 라벨이 오른쪽으로 뻗을 자리**다. 줄이면 긴 언어(포르투갈어·스페인어)에서 라벨이 화면 밖으로 잘린다.
- **라벨은 화면폭에 따라 길이가 바뀐다.** 긴 것(`code_*_label`: "안전 코드 복사")과 짧은 것(`code_*_short`: "복사")을 **둘 다 출력**해 두고 `≤640px`에서 CSS로 교체한다(`.lg`/`.sm`). 좁은 화면에서는 긴 라벨이 화면 밖으로 나가기 때문이다. 짧은 쪽은 한 단어로 유지할 것 — 포르투갈어 `Compartilhar`가 가장 길어 여기서 폭이 결정된다.
- ⚠️ **좁은 화면에서 `.pwrap`을 왼쪽으로 미는 것은 `margin`이 아니라 `transform: translateX()`다.** `margin-right`를 늘려도 480px 이하에서는 컨테이너 정렬에 걸려 **더 이상 움직이지 않는다**(실측 확인). 라벨이 오른쪽으로 넘칠 때는 translateX 값을 조정한다 — 단 너무 밀면 반대로 폰 왼쪽이 화면 밖으로 나간다(360px에서 아랍어가 그 한계다).
- ⚠️ **점선은 `border: dashed`가 아니라 배경 패턴(`repeating-linear-gradient`)으로 그린다.** 좁은 화면에서 `.pwrap`이 0.55배로 줄면 `1.5px` 테두리가 **0.8px 미만**이 되어 Safari가 아예 그리지 않는다(iOS 실기기 확인, 2026-08-07). 배경 패턴은 축소돼도 살아남는다. `border-left`/`border-top` 방식으로 되돌리지 말 것.
- 점선이 카드 밖으로 뻗어야 하므로 `.codecard`는 `overflow: visible`이고, 원형 액센트만 안쪽 `.cbg`에서 자른다. `overflow:hidden`을 되돌리면 점선이 잘린다.

## 비공개 테스트 안내 페이지 (이 저장소에서 관리)

Google Play 비공개 테스트 / 마케팅용으로 만든 **자기완결형 단일 HTML** 2종. 둘 다 CSS·JS 인라인 + **스크린샷을 base64로 내장**해 의존성이 없다. `<meta name="robots" content="noindex,nofollow">`로 검색 비노출(unlisted) 처리 — 인증 기반 비공개는 GitHub Pages 한계로 불가, **URL을 아는 사람만 접근**.

| 페이지 | URL | 용도 | 성격 |
| --- | --- | --- | --- |
| `test/index.html` | **averic.co.kr/test/** | **비공개 테스터(전문 외주 업체)** | 절차 중심 단순 가이드. 메커니즘 설명 제외, "무엇을 누르는지"만 |
| `preview/index.html` | **averic.co.kr/preview/** | **마케팅 / 인플루언서** | 앱 동작·핵심기능까지 상세 설명 |

> ⚠️ `test/`·`preview/`의 `index.html`은 **직접 수정하지 말 것.** 아래 빌드 스크립트로 생성되는 산출물이다.

### 빌드 (소스 = `_beta-test-build/`)

`_` 접두 폴더라 **웹에 게시되지 않는다**. 편집·재생성은 여기서 한다.

```
_beta-test-build/
├── build_test.py        # → ../test/index.html    (테스터용 단순 버전)
├── build_preview.py     # → ../preview/index.html  (마케팅 상세 버전)
└── img/                 # 웹용으로 축소한 스크린샷(JPEG, 폭 560) — base64 소스
    ├── settings.jpg  safety_home.jpg  dashboard.jpg  add_subject.jpg
    └── connection_management.jpg  notifications.jpg  notifications_settings.jpg
```

```bash
cd _beta-test-build
python3 build_test.py      # test/index.html 재생성
python3 build_preview.py   # preview/index.html 재생성
```

- HTML 본문·CSS는 각 스크립트의 `HTML = r"""..."""` 문자열에 들어 있다. **문구/디자인 수정은 이 문자열을 고치고 재빌드**한다.
- 이미지는 `__IMG_SETTINGS__` 같은 토큰을 `img/`의 파일에서 읽어 `data:image/jpeg;base64,...`로 치환한다.
- 스크린샷 원본(고해상도)은 앱 저장소 쪽 `~/Desktop/안부 언어별 스크린샷/한국어/`의 PNG. 교체 시 `sips -Z 560 원본.png --out img/이름.jpg -s format jpeg -s formatOptions 82`로 축소해 `img/`에 넣고 재빌드.

### 두 페이지의 내용 범위

공통 전제: **G+S(보호자가 대상자 겸함) 방식**으로 테스트하되, "G+S" 같은 내부 약어는 노출하지 않고 앱의 실제 버튼 문구("내 안전 코드 생성")나 일반어("안부 보호 활성화")로 표현한다. Android 전용. 버그 보고 메일: **l10s18bok@naver.com**.

**`test/` (테스터용) 7단계:** ①설치&권한 허용 ②안부 보호 활성화 ③서로의 코드 연결(본인 코드 금지) ④안부시각 전원 동일(오후 12~6시) ⑤매일 루틴(★핵심: 하루 1회 실행→알림 확인→스와이프 종료) ⑥경고 시 '안전확인 완료' ⑦긴급 도움 요청(선택). 5번에 **알림 2종 구분**(💗 "안부 확인이 필요합니다"=내 안부 즉시 전송 / 그 외 주의·경고·긴급=내가 지켜보는 대상자 알림, 예약시각+2h 이후 터치) 포함.

**`preview/` (마케팅):** 위 흐름 + heartbeat 자동 전송 원리, +2시간 안전망, 스와이프 종료 이유, 권한 단계, 참고 화면(알림설정·연결관리) 등 상세.

### 핵심 UI 라벨 (앱 실제 문구 — 변경 시 두 페이지 동기화)

- 안부 보호 활성화 버튼: **내 안전 코드 생성** (활성화 후 같은 자리 → **내 안전 코드 확인**)
- 안부시각 변경: **안부 푸시 알림 시각 변경**
- 경고 해소 버튼: **안전확인 완료**
- 수동 보고: **지금 바로 안전 보고하기** / 긴급: **도움이 필요해요**
- ⚠️ 제공된 설정 스크린샷은 **활성화 후** 상태("내 안전 코드 확인")라, 테스터가 처음 보는 "내 안전 코드 생성"과 다르다 — 두 페이지 모두 이 차이를 명시하고 있으니 유지할 것.

## FAQ · 사용설명 페이지 (`_faq-build/`)

앱 사용자용 페이지 **두 종**을 만든다 — `{lang}/faq.html`(문제 해결)과 `{lang}/guide.html`(안전 코드로 가족과 연결하는 방법, 애니메이션). **상세 명세는 [`_faq-build/PRD-FAQ.md`](_faq-build/PRD-FAQ.md)** — 아래는 요약이다.

> ⚠️ 폴더 이름이 `_faq-build`지만 **두 페이지를 함께 만든다.** 나누지 않은 이유는 앱 문구 추출(`app-strings.json`)·언어 목록·앱 화면 복제 CSS(`mockup.css`)를 공유하기 때문이다 — 폴더를 나누면 그 셋이 두 벌이 되어 어긋나고, 그건 FAQ가 스크린샷을 버린 이유와 같은 문제다.

```
_faq-build/                    # 게시 제외
├── PRD-FAQ.md                 # 명세 (이 폴더의 권위)
├── extract_strings.py         # 앱 저장소 번역 → JSON
├── app-strings.json           # 앱 화면 문구. 손으로 고치지 말 것
├── common.py                  # 두 빌더 공용 (언어 메타·@키 치환·CSS 로딩)
├── base.css / footer.css      # 사이트 공통 (팔레트·헤더·푸터)
├── mockup.css                 # 앱 화면 복제 — 두 페이지 공용. 값은 앱 소스와 동일
├── guide-mockup.css           # 사용설명 전용 복제 (설정·연결 화면, 하단네비, 스낵바)
├── copy/{lang}.json           # FAQ 질문·답변        + copy/README.md
├── guide-copy/{lang}.json     # 사용설명 단계 설명
├── template.html              # FAQ 골격 + 목업
├── guide-template.html        # 사용설명 골격 + 목업 3종 + 애니메이션 JS
├── build_faq.py               # → ../{lang}/faq.html
└── build_guide.py             # → ../{lang}/guide.html
```

- **CSS를 나눠 둔 이유**: 앱 화면 복제 값(기기 폭 373, `#00685E`, 코드 42px 등)이 두 템플릿에 복붙되면 한쪽만 고쳐져 어긋난다. 각 템플릿은 자기 **본문 마크업**만 갖고 CSS는 같은 파일을 인라인한다.
- **`mockup.css`를 고칠 때는 FAQ도 함께 재빌드해 확인할 것** — 두 페이지가 공유한다.
- **사용설명 단계 수는 `guide-template.html`의 `STEPS`(JS)가 정한다.** `guide-copy`의 `steps` 배열 길이가 다르면 `build_guide.py`가 exit 1 한다(번호와 화면이 따로 놀기 때문).
- **플랫폼 토글(`#pf`)은 2026-08-31에 제거됐다.** 확인 창 단계에서 Android/iPhone 탭으로 두 개의 `.dlg`(`data-pf`)를 갈아 끼우던 장치다. 앱이 iOS 전용 경고 카드를 없애 **두 플랫폼 다이얼로그가 같아지면서** 보여줄 차이가 사라졌다(NSE 트리거 2026-08-22 + 피기백 08-29로 iPhone도 탭 없이 자동 전송된다). 다시 플랫폼별 화면이 필요해지면 마크업(`.dlg[data-pf]` + `#pf`)과 JS 세 곳(`render()`의 표시 분기, 클릭 핸들러, `platform` 변수)을 함께 되살려야 한다 — ⚠️ **엘리먼트만 넣고 JS를 빠뜨리거나 그 반대면 `$('pf')`가 null이라 스크립트가 통째로 죽는다.**

- **스크린샷을 쓰지 않는다.** 앱 UI를 HTML/CSS로 재현하고, 앱 화면 문구는 앱 번역 파일에서 추출한다 — 20개 언어가 공짜이고, 앱 문구가 바뀌어도 낡지 않는다(근거는 PRD §1).
- **문장이 두 종류다.** 앱 화면 문구는 `app-strings.json`(추출), 질문·답변과 단계 설명은 `copy/`·`guide-copy/{lang}.json`(직접 작성). 카피에서 앱 문구를 인용할 때는 **`@키`**를 쓰며 **번역하지 않는다** — 번역하면 앱 화면과 어긋난다.
- **언어 목록은 `i18n/build.py`의 `META`/`ORDER`를 임포트해 쓴다.** 복제하면 어긋난다. **faq.html·guide.html의 스위처는 `build_faq.py`/`build_guide.py`가 직접 만들고 링크는 `/{code}/faq.html`로** 건다(홈으로 보내지 말 것). (`build.py`의 `patch_inplace()`는 홈 단일화 이후 미사용이다.)
- **언어 추가는 `copy/`·`guide-copy/`에 파일을 넣는 것으로 끝난다** — 빌더는 폴더에 있는 언어만 생성하며 스크립트를 고칠 필요가 없다. 스위처·`hreflang`도 같은 기준이라 없는 언어를 링크해 404를 내지 않는다.
- **홈 본문은 이제 한 곳만 고친다** — `i18n/template.html`(마크업) + `i18n/translations.json`(문구) → `python3 i18n/build.py`. 예전에는 ko/en이 손으로 쓴 페이지라 세 곳을 따로 고쳐야 했고, 그래서 조용히 어긋났다(예: `nav_guide` 링크). 2026-08-07에 ko/en 카피를 `translations.json`으로 옮겨 20개 언어를 모두 템플릿에서 생성하도록 통합했다 — 이주 직후 en은 손으로 쓴 원본과 **바이트 단위로 동일**했고 18개 언어도 무변화였다. ⚠️ **`{lang}/index.html`은 산출물이다 — 직접 수정 금지.** ⚠️ `translations.json`은 `{언어: {키: 값}}` 구조에 `indent=1`이라, 스크립트로 키를 추가할 때 형식을 유지하지 않으면 diff가 2600줄로 부푼다.
- ⚠️ **헤더 링크는 그 언어의 페이지가 실제로 있을 때만 넣는다.** 20개 언어 헤더에 먼저 링크를 걸면 아직 카피가 없는 언어가 404가 된다. FAQ·사용설명 모두 현재 20개 언어가 다 있다.
- `{lang}/faq.html`·`{lang}/guide.html`은 산출물 — 직접 수정 금지, 항상 재빌드.

```bash
cd _faq-build
python3 extract_strings.py   # 앱 문구가 바뀌었을 때만 (실패 시 exit 1)
python3 build_faq.py
python3 build_guide.py
```

## 배포 (git push)

GitHub Pages라 `main`에 push하면 자동 게시된다. **배포는 사용자가 요청할 때만** 수행한다.

```bash
cd /Users/macmini/Project/Anbu/averic-lab
git add -A
git commit -m "..."
git push        # → averic.co.kr 자동 반영
```

`test/`·`preview/`·`_beta-test-build/`는 커밋 `dd6987d`로 이미 배포되어 있다. `_` 접두 폴더는 저장소에만 보존되고 웹에는 노출되지 않는다.

## 규칙

- 응답·주석·커밋 메시지는 **한글**.
- `test/`·`preview/`는 산출물 — 항상 `_beta-test-build/`의 스크립트를 고쳐 재빌드.
- 배포는 사용자가 요청할 때만 `git push`.
